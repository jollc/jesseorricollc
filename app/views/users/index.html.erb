<div class="row">
  <div class="col-sm-8">
    <h1>Users Admin (Home)</h1>
    <h3>To add a new user, click "New User" to the right.</h3>
  </div>

  <div class="col-sm-4 text-right">
    <%= link_to "New User", new_user_path, class: 'btn btn-bright btn-sm' %>
  </div>
</div>

<ul class="list-group tips">
  <li class="list-group-item active">Helpful Tips</li>
  <li class="list-group-item">Users will gain access to this admin section and have editing capabilities on your website. Please only add users you trust.</li>
  <li class="list-group-item">Content Editors will have basic access. Web Admins will have next-level access to potentially site-breaking controls. System Admins will have complete and full access.</li>
</ul>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Email (username)</th>
      <th>Name</th>
      <th>Content Editor</th>
      <th>Web Admin</th>
      <th>System Admin</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tfoot><tr><td colspan="6"><%= paginate @users %></td></tr>
  </tfoot>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.name %></td>
        <td> - </td>
        <td class="just-icon">
          <% if user.admin == true %>
            <i class="mdi mdi-check-circle text-success"></i>
          <% else %>
            <i class="mdi mdi-close-circle text-danger"></i>
          <% end %>
        </td>
        <td class="just-icon">
          <% if user.sysadmin == true %>
            <i class="mdi mdi-check-circle text-success"></i>
          <% else %>
            <i class="mdi mdi-close-circle text-danger"></i>
          <% end %>
        </td>
        <td>
          <%= link_to "Edit", edit_user_path(user), class: 'btn btn-sm btn-success' %>
          <%= link_to "Delete", user, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
